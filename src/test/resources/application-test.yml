spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/opsflow
    username: opsflow
    password: opsflow

  jpa:
    hibernate:
      ddl-auto: none


  flyway:
    enabled: true

logging:
  level:
    org.hibernate.SQL: warn